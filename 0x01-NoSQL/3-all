// This script lists all documents in the 'school' collection
// Connect to the database passed as an option
db = db.getSiblingDB('<db_name>');
// Find and print all documents in the 'school' collection
db.school.find().forEach(printjson);
